/**
 * This class handles the categories
 * @type {*}
 */
window.themingStore.models.TemplateTypeModel = window.themingStore.models.AbstractModel.extend({
  /**
   * the type of the model, required for save operations
   */
  type: 'template_type'
});